# Trading bot configuration (headless bot).
# Every value here can be overridden with an environment variable (see README).
# Secrets (API keys) must NOT go in this file — use env vars instead.
#
# "bp" = basis point = 1/100th of a percent. 100bp = 1%, so 0.0025 = 25bp.

feed:
  exchange: mexc       # mexc | bybit | okx | binance
  symbol: BTC_USDT     # single-symbol CLI bot uses this
  # The backend watches all of these at once over ONE connection.
  #
  # These are MEASURED, not guessed. A coin must print ~2 trades inside the
  # lookback window or its impulse reads 0.000% forever and it can never fire.
  # On MEXC that rules out most majors: XRP prints 0.34/s, DOGE 0.29/s,
  # BNB 0.11/s, LTC 0.10/s. Re-measure with:
  #     python scripts/pick_symbols.py --lookback 2
  symbols:
    - BTC_USDT       # 5.6 trades/s, $5.5B 24h
    - ETH_USDT       # 3.7 trades/s, $2.5B
    - PEPE_USDT      # 2.1 trades/s, $110M
    - HYPE_USDT      # 1.5 trades/s, $231M
    - SOL_USDT       # 1.2 trades/s, $1.1B
  market: futures      # perps lead spot in price discovery, and are cheaper
  stream: trade        # trade/aggTrade = prints | bookTicker = top of book
                       # NOTE: Binance geo-blocks several regions (HTTP 451).
                       # The headless bot watches ONE symbol; the web dashboard
                       # scans many at once.

strategy:
  # 2s rather than 1s: at 1s only BTC, ETH and PEPE print often enough to be
  # measurable on MEXC. 2s admits SOL and HYPE too, for a modest loosening of
  # the velocity filter. Shorter = stricter but fewer tradeable coins.
  lookback_seconds: 2.0     # shorter window = stricter velocity filter
  allow_shorts: true        # trade downward impulses as well as up
  entry_threshold: 0.0025   # ±0.25% inside the window (25bp) to enter
  take_profit: 0.006        # +0.60% — well clear of the ~18bp round trip
  stop_loss: 0.0025         # -0.25%, cut fast
  reversal_exit: 0.0015     # bail if momentum flips 0.15%
  reversal_window: 1.0      # window (s) for the reversal check
  max_hold_seconds: 45.0    # close a stalled position after this long
  cooldown_seconds: 3.0     # wait after an exit before re-entering

risk:
  # Many coins share one book. order_size_pct must leave room for the other
  # positions: at 0.95 the first entry takes almost everything and the cap
  # below becomes meaningless, because nothing is left to open with.
  max_concurrent_positions: 3
  # "equity" keeps every position the same size. "cash" sizes off remaining
  # cash, which compounds down and gives whichever coin fires first a much
  # larger bet than the third — arrival order is not signal quality.
  position_sizing: equity
  order_size_pct: 0.25       # of equity, so 3 x 25% = 75% deployed
  min_notional: 10.0         # skip orders smaller than this
  daily_loss_limit_pct: 0.05 # halt after -5% on the day

broker:
  mode: paper                # "paper" (simulated) or "live" (real money — stub)
  starting_cash: 10000.0     # paper balance in quote currency (USDT)
  # MEXC futures placed through the API cost 0.08% taker (Jun 2026). The
  # 0%/0.01% shown in the MEXC app does NOT apply to API orders.
  fee_bps: 8.0
  slippage_bps: 2.0          # assumed slippage per fill

server:
  # The backend runs the engine independently of any browser. Loopback by
  # default; binding to a public address REQUIRES TB_API_TOKEN, because this
  # API can start and stop trading.
  host: 127.0.0.1
  port: 8787
  autostart: false           # start trading the moment the process boots
  state_file: state.json     # session survives a restart
